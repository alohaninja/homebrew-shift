# homebrew-shift

Homebrew tap for [SHIFT](https://github.com/alohaninja/shift) (`shift-ai`).

## Install

```bash
brew tap alohaninja/shift
brew install shift-ai
```

## Usage

```bash
shift-ai --version
shift-ai -m economy -o report < request.json
shift-ai gain
```
